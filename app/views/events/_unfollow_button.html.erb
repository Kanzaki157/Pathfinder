<%#= form_with(url: user_relationships_path(user.id), method: :delete, local: false) do |f| %>
  <%#= f.submit 'フォロー外す', class: 'btn btn-danger' %>
<%# end %>
<%= link_to 'フォローを外す', user_relationships_path(user.id), method: :delete %>